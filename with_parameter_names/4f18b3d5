transferFrom(address _from,address _to,uint _amount);transferFrom(address _from,address _recipient,uint _amount);transferFrom(address from,address to,uint value);transferFrom(address sendingTokenHolder,address receivingTokenHolder,uint amount);transferFrom(address _from,address _to,uint _value);transferFrom(address from,address to,uint256 amount);transferFrom(address fromAddress,address toAddress,uint256 amount);transferFrom(address src,address dst,uint amount);transferFrom(address sender,address receiver,uint amount);transferFrom(address from,address to,uint256 value);transferFrom(address _from,address _recipient,uint256 _value);transferFrom(address from,address to,uint balance);transferFrom(address _from,address _to,uint256 _amountBabz);transferFrom(address,address,uint256);transferFrom(address src,address dst,uint wad);transferFrom(address _from,address _to,uint256 _tokenId);transferFrom(address from,address to,uint amount);transferFrom(address _from,address _to,uint256 _value);transferFrom(address from,address to,uint tokens);transferFrom(address _from,address _to,uint256 _amount)