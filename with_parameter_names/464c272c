transfer(address _to,uint value);transfer(address _to,uint256 _value);transfer(address _recipient,uint _amount);transfer(address _to,uint256 _amount);transfer(address _to,uint256 _tokenId);transfer(address receiver,uint amount);transfer(address _destinationAddress,uint256 _attotokens);transfer(address to,uint tokens);transfer(address to,uint amount);transfer(address _to,uint256 _amountPower);transfer(address to,uint256 value);transfer(address _to,uint256 _fund);transfer(address _to,uint _value);transfer(address to,uint value);transfer(address dst,uint wad);transfer(address _to,uint _fromAmount);transfer(address _to,uint256 _amountBabz);transfer(address recipient,uint256 amount);transfer(address to,uint _value);transfer(address _recipient,uint256 _value);transfer(address to,uint256 etherValue);transfer(address toAddress,uint256 amount);transfer(address _payee,uint _payment);transfer(address receivingTokenHolder,uint amount);transfer(address _to,uint _amount);transfer(address recipient,uint amount);transfer(address _from,address _to);transfer(address dst,uint amount);transfer(address a,uint x);transfer(address _recipient,uint _value);transfer(address,uint256);transfer(address _to,);transfer(address dest,uint wad);transfer(address to,uint256 amount)